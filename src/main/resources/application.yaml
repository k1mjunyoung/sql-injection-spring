spring:
  application:
    name: sql-injection-spring
  datasource:
    url: jdbc:mysql://localhost:3306/sql_injection_spring?serverTimezone=Asia/Seoul
    username: root
    password: root12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  sql:
    init:
      encoding: UTF-8


logging.level:
  org.hibernate.SQL: debug